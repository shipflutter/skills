# Android Appfile (TEMPLATE) — replace placeholders with your real values.
json_key_file(ENV["SUPPLY_JSON_KEY"] || "fastlane/play-store-service-account.json")
package_name(ENV.fetch("PLAYSTORE_PACKAGE_NAME", "com.example.myapp"))
